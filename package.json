{
  "name": "react-aws-lambdalith-template",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "infra"
  ],
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "npm run build --workspace=@starter/shared && npm run build --workspace=@starter/api && npm run build --workspace=@starter/web && npm run build --workspace=@starter/infra",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "deploy": "npm run build && npm run deploy --workspace=@starter/infra",
    "destroy": "npm run destroy --workspace=@starter/infra",
    "bootstrap": "npm run bootstrap --workspace=@starter/infra"
  },
  "devDependencies": {
    "@types/node": "^26.2.0"
  }
}
